import {User} from "../entity/User";
import {supabase} from "../../data/source/network/Supabase";

export class UploadCase {
    public uploadUserInfo = async (user: User): Promise<User> => {
        // supabase()

        return
    };
}